# COMPILATION

## Node
Node 24 LTS or later is required (see `.nvmrc` / `engines.node` in `package.json`).

## Windows
You need to install the npm package `windows-build-tools`
## Linux or Mac OS
You need the packages `gcc`, `make` and `build-essential`
